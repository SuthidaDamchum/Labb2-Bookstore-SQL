<UserControl x:Class="BookStore_Presentation.Views.InventoryByStoreView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:UI="clr-namespace:BookStore_Presentation.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=UI:InventoryByStoreViewModel, IsDesignTimeCreatable=True}">



    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="3*"/>

        </Grid.ColumnDefinitions>

        <!-- Lista butiker -->
        <ListBox ItemsSource="{Binding Stores}"
                 SelectedItem="{Binding SelectedStore}"
                 DisplayMemberPath="StoreName"/>
        
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <!-- Buttons -->
                <RowDefinition Height="*"/>
                <!-- DataGrid -->
            </Grid.RowDefinitions>

            <!-- Buttons Row -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Grid.Row="0" Margin="10,0,0,300">
                <Button Content="Add Quantity" Width="103" Height="25" Margin="0,0,10,0"/>
                <Button Content="Decrease Quantity" Width="105" Height="25"/>
            </StackPanel>


            <!-- DataGrid med lagersaldo -->
        <DataGrid ItemsSource="{Binding Inventory}" Grid.Column="1" AutoGenerateColumns="False" Margin="10,50,0,0">
         
            <DataGrid.Columns>
                <DataGridTextColumn Header="ISBN" Binding="{Binding ISBN}" />
                <DataGridTextColumn Header="Title" Binding="{Binding Title}" />
                <DataGridTextColumn Header="Price" Binding="{Binding Price}" />
                <DataGridTextColumn Header="Quantity" Binding="{Binding Quantity}" />
            </DataGrid.Columns>
            
            
        </DataGrid>
        </Grid>
    </Grid>
</UserControl>
